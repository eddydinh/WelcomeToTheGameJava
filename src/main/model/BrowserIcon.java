package model;

import java.awt.*;

public class BrowserIcon extends Icon {
    public BrowserIcon(int iconX, int iconY, int iconWidth, int iconHeight, String iconName, Image iconImg) {
        super(iconX, iconY, iconWidth, iconHeight, iconName, iconImg);
    }
}
